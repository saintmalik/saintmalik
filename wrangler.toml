# Cloudflare Pages. Dashboard: Build command `bash build.sh`, output `output`.
# pages_build_output_dir is required for Pages to accept this file.
# Do not set [assets] — that is Workers-only and Pages skips the file.

name = "saintmalik"
pages_build_output_dir = "output"
compatibility_date = "2026-09-16"
